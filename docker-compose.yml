services:
  fpt-coding-agent:
    build: .
    container_name: fpt_coding_agent
    restart: unless-stopped
    ports:
      - "5000:5000"
    # Toàn bộ cấu hình lấy từ .env (xem .env.example)
    env_file:
      - .env
