version: '3.8'

services:
  fpt-ai-chatbot:
    build: .
    container_name: fpt_ai_chatbot
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
